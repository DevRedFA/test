###
#   Database Settings
###
spring:
  datasource:
    data: classpath:data-test.sql
    url: jdbc:h2:mem:for_tests;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    platform: h2
    username: sa
    password:
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update